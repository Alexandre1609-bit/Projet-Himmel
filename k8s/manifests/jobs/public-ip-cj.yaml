apiVersion: batch/v1
kind: CronJob
metadata:
  name: duckdns-updater
  namespace: network
spec:
  schedule: "*/15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      activeDeadlineSeconds: 300
      template:
        spec:
          containers:
            - name: duckdns-curl
              image: curlimages/curl:8.21.0
              env:
                - name: DUCKDNS_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: dns-key
                      key: token
              command:
                - /bin/sh
                - -c
                - "curl -k -s -m 10 'https://www.duckdns.org/update?domains=projet-himmel&token=$DUCKDNS_TOKEN&ip='"
          restartPolicy: OnFailure
